# yaml-language-server: $schema=https://raw.githubusercontent.com/datreeio/CRDs-catalog/refs/heads/main/onepassword.com/onepassworditem_v1.json
apiVersion: onepassword.com/v1
kind: OnePasswordItem
metadata:
  name: onepassword-authentik-email
spec:
  # Reuses the same SES SMTP credentials as the ses-freenas item (single
  # source of truth for these creds - shared with whatever else on the NAS
  # already sends mail through this SES SMTP user). Its built-in
  # username/password fields (AKIAJANFV4UJHJJL7DXQ / SMTP password) become
  # the generated Secret's `username`/`password` keys.
  itemPath: "vaults/evjh25ux2mnoufaut6qhvm2pdi/items/txtdnznpwqkqd4k7uiwfj5iy34"
