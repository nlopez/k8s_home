service:
  type: LoadBalancer

ingress:
  enabled: false

persistence:
  config:
    enabled: true
    accessMode: ReadWriteOnce
    size: 10Gi
    storageClass: rook-ceph-block
  media:
    enabled: true
    existingClaim: media-jellyfin
