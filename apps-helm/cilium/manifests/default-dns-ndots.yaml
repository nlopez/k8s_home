apiVersion: admissionregistration.k8s.io/v1
kind: MutatingAdmissionPolicy
metadata:
  annotations:
    argocd.argoproj.io/tracking-id: >-
      cilium:admissionregistration.k8s.io/MutatingAdmissionPolicy:kube-system/default-dns-ndots
  name: default-dns-ndots
spec:
  matchConstraints:
    resourceRules:
    - apiGroups:
      - ''
      apiVersions:
      - v1
      operations:
      - CREATE
      resources:
      - pods
    - apiGroups:
      - apps
      apiVersions:
      - v1
      operations:
      - CREATE
      - UPDATE
      resources:
      - deployments
      - statefulsets
      - daemonsets
    - apiGroups:
      - batch
      apiVersions:
      - v1
      operations:
      - CREATE
      - UPDATE
      resources:
      - cronjobs
      - jobs
  mutations:
  - jsonPatch:
      expression: |-
        object.kind == "Pod" ? [
          JSONPatch{
            op: has(object.spec.dnsConfig) ? "replace" : "add",
            path: "/spec/dnsConfig",
            value: {
              "options": [
                {
                  "name": "ndots",
                  "value": "1"
                }
              ]
             }
          }
        ] : object.kind == "CronJob" ? [
          JSONPatch{
            op: has(object.spec.jobTemplate.spec.template.spec.dnsConfig) ? "replace" : "add",
            path: "/spec/jobTemplate/spec/template/spec/dnsConfig",
            value: {
              "options": [
                {
                  "name": "ndots",
                  "value": "1"
                }
              ]
             }
          }
        ] : [
          JSONPatch{
            op: has(object.spec.template.spec.dnsConfig) ? "replace" : "add",
            path: "/spec/template/spec/dnsConfig",
            value: {
              "options": [
                {
                  "name": "ndots",
                  "value": "1"
                }
              ]
             }
          }
        ]
    patchType: JSONPatch
  reinvocationPolicy: IfNeeded
