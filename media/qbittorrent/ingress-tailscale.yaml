apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: qbittorrent-tailscale
  namespace: media
spec:
  tls:
  - hosts:
    - rough-pig
  defaultBackend:
    service:
      name: qbittorrent
      port:
        name: http
  ingressClassName: tailscale
