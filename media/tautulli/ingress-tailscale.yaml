apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: tautulli-tailscale
  namespace: media
spec:
  tls:
  - hosts:
    - bustling-goat
  defaultBackend:
    service:
      name: tautulli
      port:
        name: http-tautulli
  ingressClassName: tautulli
