---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: overseerr
  namespace: media
  annotations:
    fluxcd.io/automated: 'true'
    fluxcd.io/tag.overseerr: 'regex:^v2\.[0-9\.]+-ls[0-9]+$'
spec:
  serviceName: overseerr
  replicas: 1
  selector:
    matchLabels:
      app: overseerr
  template:
    metadata:
      labels:
        app: overseerr
    spec:
      volumes:
      - name: overseerr
        iscsi:
          iqn: iqn.2014-09.com.radoncanyon:nas1:overseerr
          targetPortal: 192.168.220.149
          lun: 1
          fsType: ext2
      containers:
      - name: overseerr
        image: lscr.io/linuxserver/overseerr:version-v1.29.1
        volumeMounts:
        - name: overseerr
          subPath: config
          mountPath: /config
        env:
        - name: PUID
          value: '1001'
        - name: PGID
          value: '1001'
        - name: TZ
          value: 'America/New_York'
        ports:
        - name: overseerr
          containerPort: 5055
        readinessProbe:
          tcpSocket:
            port: overseerr
          initialDelaySeconds: 5
          periodSeconds: 10
        livenessProbe:
          tcpSocket:
            port: overseerr
          initialDelaySeconds: 15
          periodSeconds: 20
        env:
        - name: TAUTULLI_API_KEY
          valueFrom:
            secretKeyRef:
              name: overseerr
              key: TAUTULLI_API_KEY
        - name: TAUTULLI_URI
          value: 'http://127.0.0.1:8181'
