apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: radarr4k
  namespace: media
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: radarr4k
    spec:
      volumes:
      - name: radarr4k
        iscsi:
          iqn: iqn.2014-09.com.radoncanyon:nas1:radarr4k
          targetPortal: nas1-storage.radoncanyon.com
          lun: 0
      - name: media
        persistentVolumeClaim:
          claimName: media
      containers:
      - name: radarr4k
        image: linuxserver/radarr:142
        volumeMounts:
        - name: radarr4k
          subPath: config
          mountPath: /config
        - name: media
          mountPath: /media
        env:
        - name: PUID
          value: '1001'
        - name: PGID
          value: '1001'
        - name: TZ
          value: 'America/New_York'
        ports:
        - name: radarr
          containerPort: 7878
        readinessProbe:
          tcpSocket:
            port: radarr
          initialDelaySeconds: 5
          periodSeconds: 10
        livenessProbe:
          tcpSocket:
            port: radarr
          initialDelaySeconds: 15
          periodSeconds: 20
