apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: nzbget
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: nzbget
    spec:
      nodeSelector:
        kubernetes.io/hostname: biggs.radoncanyon.com
      volumes:
      - name: nzbget
        hostPath:
          path: /dockerfs/nzbget
      - name: downloads-nas2
        persistentVolumeClaim:
          claimName: downloads-nas2
      containers:
      - name: nzbget
        image: linuxserver/nzbget:124
        volumeMounts:
        - name: nzbget
          subPath: config
          mountPath: /config
        - name: nzbget
          subPath: intermediate
          mountPath: /intermediate
        - name: downloads-nas2
          mountPath: /downloads/nas2
        env:
        - name: PUID
          value: '1001'
        - name: PGID
          value: '1001'
        - name: TZ
          value: 'America/New_York'
        ports:
        - name: nzbget
          containerPort: 6789
        readinessProbe:
          tcpSocket:
            port: nzbget
          initialDelaySeconds: 5
          periodSeconds: 10
        livenessProbe:
          tcpSocket:
            port: nzbget
          initialDelaySeconds: 15
          periodSeconds: 20
