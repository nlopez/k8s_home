{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended"
  ],
  "dependencyDashboard": true,
  "enabledManagers": [
    "custom.regex",
    "helm-values",
    "github-actions"
  ],
  "customManagers": [
    {
      "customType": "regex",
      "description": "Update Helm chart versions in bootstrap ApplicationSet list entries (HTTPS)",
      "fileMatch": [
        "bootstrap/apps-helm.yaml"
      ],
      "matchStrings": [
        "- name:\\s*(?<appName>[^\\n]+)\\s*\\n\\s*repoURL:\\s*['\"]?(?<registryUrl>https?://[^\\s'\"]+)['\"]?\\s*\\n\\s*chart:\\s*['\"]?(?<depName>[^\\s'\"]+)['\"]?\\s*\\n\\s*targetRevision:\\s*['\"]?(?<currentValue>[^\\s'\"]+)['\"]?"
      ],
      "datasourceTemplate": "helm",
      "registryUrlTemplate": "{{{registryUrl}}}",
      "versioningTemplate": "semver-coerced"
    },
    {
      "customType": "regex",
      "description": "Update OCI-hosted Helm chart versions in bootstrap ApplicationSet list entries",
      "fileMatch": [
        "bootstrap/apps-helm.yaml"
      ],
      "matchStrings": [
        "- name:\\s*(?<appName>[^\\n]+)\\s*\\n\\s*repoURL:\\s*['\"]?oci://(?<depName>[^\\s'\"]+)['\"]?\\s*\\n\\s*targetRevision:\\s*['\"]?(?<currentValue>[^\\s'\"]+)['\"]?"
      ],
      "datasourceTemplate": "docker",
      "versioningTemplate": "semver-coerced"
    },
    {
      "customType": "regex",
      "description": "Update container images in Kubernetes manifests",
      "fileMatch": [
        "apps/.+\\.ya?ml$"
      ],
      "matchStrings": [
        "(?m)^\\s*image:\\s*(?<depName>[^:@\\s]+(?:/[^:@\\s]+)*)(?::(?<currentValue>[^@\\s]+))?(?:@(?<currentDigest>sha256:[a-f0-9]+))?\\s*$"
      ],
      "datasourceTemplate": "docker"
    }
  ],
  "packageRules": [
    {
      "description": "Group Helm chart updates from bootstrap/apps-helm.yaml",
      "matchFileNames": [
        "bootstrap/apps-helm.yaml"
      ],
      "groupName": "helm chart updates"
    },
    {
      "description": "Group container image updates from manifests and Helm values",
      "matchManagers": [
        "kubernetes",
        "helm-values"
      ],
      "groupName": "container image updates"
    },
    {
      "description": "Group container image updates from non-Helm app manifests",
      "matchManagers": [
        "regex"
      ],
      "matchFileNames": [
        "apps/**"
      ],
      "groupName": "container image updates"
    },
    {
      "description": "Use loose versioning for linuxserver images which use version-X.Y.Z tag format",
      "matchPackageNames": [
        "ghcr.io/linuxserver/**",
        "lscr.io/linuxserver/**"
      ],
      "versioning": "loose"
    },
    {
      "description": "Use calver versioning for media-toolbox which uses YYYY.M.D.HHmmss tags",
      "matchPackageNames": [
        "docker.io/nlopez/media-toolbox"
      ],
      "versioning": "calver"
    },
    {
      "description": "Use loose versioning for app images with non-semver tags",
      "matchPackageNames": [
        "boredazfcuk/icloudpd",
        "docker.io/brainicism/bgutil-ytdlp-pot-provider",
        "caomingjun/warp",
        "ghcr.io/shaarli/shaarli",
        "registry.k8s.io/sig-storage/snapshot-controller"
      ],
      "versioning": "loose"
    },
    {
      "description": "Ignore untagged or latest-pinned images that cannot meaningfully be updated",
      "matchPackageNames": [
        "bitnami/kubectl"
      ],
      "enabled": false
    },

    {
      "description": "Group GitHub Actions updates",
      "matchManagers": [
        "github-actions"
      ],
      "groupName": "github actions updates"
    }
  ]
}
