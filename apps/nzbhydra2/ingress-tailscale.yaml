apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: nzbhydra2-tailscale
spec:
  tls:
  - hosts:
    - billowy-voice
  defaultBackend:
    service:
      name: nzbhydra2
      port:
        name: http
  ingressClassName: tailscale
