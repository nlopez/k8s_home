---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nzbhydra2
  labels:
    app: nzbhydra2
spec:
  storageClassName: rook-ceph-block
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
