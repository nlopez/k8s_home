apiVersion: v1
kind: Service
metadata:
  name: pms
  labels:
    app: pms
    version: v1
spec:
  type: LoadBalancer
  selector:
    app: pms
    version: v1
  ports:
  - name: https
    port: 32400
    targetPort: 32400
