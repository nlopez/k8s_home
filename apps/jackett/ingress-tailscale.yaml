---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: jackett-tailscale
spec:
  tls:
  - hosts:
    - jazzy-hat
  defaultBackend:
    service:
      name: jackett
      port:
        name: http
  ingressClassName: tailscale
