---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: sonarr
spec:
  tls:
  - hosts:
    - sonarr
  defaultBackend:
    service:
      name: sonarr
      port:
        name: http
  ingressClassName: tailscale
