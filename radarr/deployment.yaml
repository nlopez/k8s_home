apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: radarr
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: radarr
    spec:
      nodeSelector:
        kubernetes.io/hostname: wedge.radoncanyon.com
      volumes:
      - name: media
        hostPath:
          path: /mnt/media
      - name: radarr
        hostPath:
          path: /dockerfs/radarr
      - name: downloads
        hostPath:
          path: /mnt/media/downloads/nas1
      containers:
      - name: radarr
        image: linuxserver/radarr
        volumeMounts:
        - name: radarr
          subPath: config
          mountPath: /config
        - name: media
          subPath: movies
          mountPath: /movies
        - name: downloads
          mountPath: /downloads
        env:
        - name: PUID
          value: '1001'
        - name: PGID
          value: '1001'
        - name: TZ
          value: 'America/New_York'
        ports:
        - name: radarr
          containerPort: 7878
        livenessProbe:
          httpGet:
            path: /
            port: radarr
          initialDelaySeconds: 60
          periodSeconds: 5

