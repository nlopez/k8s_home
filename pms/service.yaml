apiVersion: v1
kind: Service
metadata:
  name: pms
spec:
  externalTrafficPolicy: Local
  selector:
    app: pms
  type: NodePort
  ports:
  - name: pms
    port: 32400
    targetPort: 32400
    nodePort: 32400
