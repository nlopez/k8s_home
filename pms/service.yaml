apiVersion: v1
kind: Service
metadata:
  name: pms
  annotations:
    kube-router.io/service.dsr: "tunnel"
spec:
  selector:
    app: pms
  type: ClusterIP
  externalIPs:
  - 10.69.96.2
  ports:
  - name: pms
    port: 32400
    targetPort: 32400
  - name: webtools
    port: 33400
    targetPort: 33400
