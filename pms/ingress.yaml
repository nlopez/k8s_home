apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: pms-ingress
  annotations:
  #   kubernetes.io/tls-acme: "true"
    kubernetes.io/ingress.class: "nginx"
  #   ingress.kubernetes.io/auth-url: "http://10.103.59.104:4180"
spec:
  # tls:
  # - hosts:
  #   - plex.desertbluffs.com
  #   secretName: pms-service-tls
  rules:
  - host: plex.test
    http:
      paths:
      - path: /
        backend:
          serviceName: pms
          servicePort: 32400
