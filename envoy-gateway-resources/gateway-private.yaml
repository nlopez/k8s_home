apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: private
  annotations:
    lbipam.cilium.io/ips: "10.43.64.10"
spec:
  gatewayClassName: private
  addresses: []
  listeners:
    - name: http
      protocol: HTTP
      port: 80
