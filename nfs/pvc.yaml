---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: scratch
  namespace: media
spec:
  selector:
    matchLabels:
      name: scratch
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Pi
